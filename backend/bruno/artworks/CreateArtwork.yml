info:
  name: CreateArtwork
  type: http
  seq: 1
  tags:
    - artworks

http:
  method: POST
  url: "{{api_url}}/artworks"
  body:
    type: json
    data: |-
      {
        "name": "Frontend draft",
        "category": "Illustration",
        "styles": ["Watercolor"],
        "description": "A watercolor illustration commission.",
        "artwork_samples": [
          {
            "image_url": "https://example.com/frontend-draft.png"
          }
        ],
        "minimum_deadline_days": 14,
        "price_satang": 50000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Create portfolio artwork owned by the authenticated artist.
