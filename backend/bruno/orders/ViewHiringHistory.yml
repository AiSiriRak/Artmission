info:
  name: ViewHiringHistory
  type: http
  seq: 1
  tags:
    - orders

http:
  method: GET
  url: "{{api_url}}/orders/history"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{api_url}}/orders/history"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "$schema": "",
            "orders": ""
          }
  - name: default Response
    description: Error
    request:
      url: "{{api_url}}/orders/history"
      method: GET
    response:
      headers:
        - name: Content-Type
          value: application/problem+json
      body:
        type: json
        data: |-
          {
            "$schema": "",
            "detail": "",
            "errors": "",
            "instance": "",
            "status": 0,
            "title": "",
            "type": ""
          }

docs: View the authenticated customer's hiring history
