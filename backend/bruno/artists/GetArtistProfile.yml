info:
  name: GetArtistProfile
  type: http
  seq: 1
  tags:
    - artists

http:
  method: GET
  url: "{{api_url}}/artists/{{artist_id}}"

runtime:
  variables:
    - name: artist_id
      value: 615e6dd6-2648-40ba-ae0f-ee3efac0d9ff

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Get a public artist profile by artist user ID.
