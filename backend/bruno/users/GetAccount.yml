info:
  name: GetAccount
  type: http
  seq: 1
  tags:
    - users

http:
  method: GET
  url: "{{api_url}}/users/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get the authenticated user's account information.
